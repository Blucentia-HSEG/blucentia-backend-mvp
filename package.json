{
  "name": "hseg-analytics-dashboard",
  "version": "1.0.0",
  "description": "HSEG Workplace Culture Analytics Dashboard",
  "main": "app.py",
  "scripts": {
    "start": "python app.py",
    "dev": "python app.py",
    "build": "python utils/merge_json.py && echo Build complete - dataset ready for production",
    "prebuild": "pip install -r requirements.txt",
    "merge-data": "python utils/merge_json.py",
    "split-data": "python utils/split_json.py",
    "setup": "pip install -r requirements.txt",
    "clean": "python -c \"import os; os.remove('hseg_final_dataset.json') if os.path.exists('hseg_final_dataset.json') else None; print('Cleaned merged dataset')\""
  },
  "keywords": [
    "hseg",
    "analytics",
    "dashboard",
    "workplace-culture",
    "flask",
    "data-visualization"
  ],
  "author": "Your Organization",
  "license": "MIT",
  "engines": {
    "python": ">=3.8"
  }
}
